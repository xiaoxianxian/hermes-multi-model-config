## 2026-06-20

### 11:55 - 最终整理 & GitHub 推送

1. **修正 config.yaml 实际配置**：`hermes doctor` 发现 `max_turns` 仍是 90、`fallback_model` 被注释。已修正为 `max_turns: 50`，启用 `fallback_model: deepseek/deepseek-v4-flash`。
2. **生成项目 README.md**。
3. **初始化 Git 并推送到 GitHub**：`https://github.com/xiaoxianxian/hermes-multi-model-config`
   - `README.md` - 项目说明
   - `hermes-new-machine-guide.md` - 详细配置指南
   - `check-hermes-config.md` - 诊断检查清单
   - `scripts/setup-hermes-multi-model.sh` - 一键配置脚本
   - `.gitignore` - 排除 API Key 等敏感文件

### 12:25 - 打包项目供 Hermes 检查

将项目文件打包为 `hermes-multi-model-config.tar.gz`（约 10KB），包含：
- `.gitignore` / `README.md` / `check-hermes-config.md` / `hermes-new-machine-guide.md`
- `scripts/setup-hermes-multi-model.sh`（复制件）

### 12:45 - 修复配置问题 & GitHub 推送

**修复 5 个问题：**

1. **`config.yaml` model 中删除冗余 `api_key`**：`model:` 字典里的 `api_key` 字段已被移除，API Key 统一由 `providers:` 统一管理。
2. **`providers.agnes` 添加 `transport: chat_completions`**：与 `deepseek`、`kimi-coding` 保持一致。
3. **验证 fallback_model**：`hermes doctor` 确认 `fallback_model` 正确解析到 `deepseek/deepseek-v4-flash`。
4. **`setup-hermes-multi-model.sh` sed 转义修复**：新增 `escape_sed()` 函数，对 API Key 中的特殊字符（`/`, `-`, `+` 等）进行转义，防止 sed 替换失败。
5. **验证 API Key 未被截断**：确认 `~/.hermes/config.yaml` 中 Agnes Key（52 字符）和 DeepSeek Key（36 字符）均为完整值。

**脚本还移除了两处多余的 `api_key` 字段**（第 176 行和第 218 行的 `model:` 块中），避免与 `providers.` 重复。

### 15:00 - 最终检查结果确认

Hermes 完成全面配置检查，结论如下：

**✅ 已全部修复的 9 个问题：**
1. `model.api_key` 冗余字段 → 已删除，`model:` 只剩 `base_url`, `default`, `provider`
2. Agnes provider 缺少 `transport` → 已添加 `chat_completions`
3. `custom_providers` 残留 → `grep` 无匹配，已清除
4. `main.py` 补丁 → `_has_any_provider_configured` 已扫描 `custom_providers`
5. `sed` 特殊字符转义 → `escape_sed()` 函数已添加
6. API Key 完整性 → 两个 Key 都是完整值
7. `model:` 格式 → dict 格式，`provider: custom`
8. `fallback_model` → 指向 `deepseek/deepseek-v4-flash`
9. `max_turns` → 值为 50

**🔍 边缘情况说明（非 bug）：**
- **Kimi provider 未配置**：config.yaml 中只有 `deepseek` 和 `agnes` 两个 provider，`kimi-coding` 不在其中。文档和脚本提及了 Kimi，但实际未使用，不影响。
- **`/model` 切换持久化**：切换到其他 provider 后 `model.provider` 可能被覆盖为 `deepseek` 等非 custom 值，这是 Hermes 的设计行为，不影响使用。
- **`fallback_model` 解析**：`resolve_provider("deepseek")` 能从 `providers:` 字典中找到 deepseek provider，已通过 `hermes doctor` 验证。

**结论：配置已完善，可以正常使用。**
